import 'package:flutter/material.dart';

class AppColors {
  // Brand (iOS Native Feel)
  static const primary = Color(0xFF007AFF); // Apple System Blue
  static const systemBlue = Color(0xFF007AFF); // Apple System Blue
  static const systemRed = Color(0xFFFF3B30); // Apple System Red for SOS/Destructive
  static const systemGreen = Color(0xFF34C759);
  static const systemGray = Color(0xFF8E8E93);
  
  // Backgrounds
  // Pure white for screens that aren't grouped, grouped bg for Settings-like lists
  static const pureWhite = Color(0xFFFFFFFF); 
  static const background = Color(0xFFF2F2F7); // iOS System Grouped Background Light
  static const cardBg = Color(0xFFFFFFFF); // Crisp white for cards/tiles
  static const inputBg = Color(0xFFE5E5EA); // Search bar background
  
  // Typography
  static const textPrimary = Color(0xFF000000); // Profound black
  static const textSecondary = Color(0xFF8E8E93); // iOS System Gray
  
  // Structural
  static const border = Color(0xFFC6C6C8); // Delicate iOS separator line
}
